# build.toml — конфигурация сборки .exe через PyInstaller

[pyinstaller]
script = "ui.py"
name = "1C_to_CK-11"

[[pyinstaller.datas]]
src = "modules"
dest = "modules"

[[pyinstaller.datas]]
src = "config.json"
dest = "."

pyinstaller.hiddenimports = [
    "main",
    "modules.hierarchy_parser",
    "modules.xml_generator",
    "modules.config_manager",
    "modules.file_manager",
    "modules.logger_manager"
]

pyinstaller.noconsole = true
pyinstaller.onefile = true
pyinstaller.upx = true

# Обязательная секция для build.py
[build]
author = "EvTK"
# Можно добавить другие поля, если build.py их использует:
# output_dir = "dist"
# platform = "windows"